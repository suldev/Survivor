1 CLS

LOCATE 3, 1
INPUT "PLEASE ENTER YOUR PROCESSOR SPEED (IN GHz): ", SPEED

PRINT SPEED; "GIGAHERTZ, IS THIS CORRECT";
INPUT ANSWER$
ANSWER$ = UCASE$(MID$(ANSWER$, 1, 1))

IF ANSWER$ = "N" THEN GOTO 1

SPEED = SPEED * 1073741824

CLS

SHELL "dir u77.z77 > dir.txt"
OPEN "dir.txt" FOR INPUT AS #1
DO WHILE NOT EOF(1)
  LINE INPUT #1, l$
  IF INSTR(l$, "Directory") > 0 THEN EXIT DO
LOOP
CLOSE #1
ROOT$ = MID$(l$, 15)
KILL "DIR.TXT"

CHDIR ROOT$ + "\UTIL"
OPEN "PROC.UTL" FOR OUTPUT AS #1
PRINT #1, SPEED
CLOSE #1

CHDIR ROOT$

CHAIN "SURVIVOR"

